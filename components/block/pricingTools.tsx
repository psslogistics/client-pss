"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowRight, Calculator, Check, ChevronDown, MapPin, Plus, RefreshCw, Trash2, Truck } from "lucide-react";
import type { SavedAddressRecord } from "@/lib/client-operations-data";
import { calculateRates, chargeableWeight, checkServiceability, formatINR, isValidPincode, volumetricWeight, type BoxLine, type PaymentMode, type ShipmentType } from "@/lib/client-pricing-data";
import { cn } from "@/lib/utils";
import { pssApi } from "@/lib/pss-api";

const inputClass = "h-10 w-full rounded-lg border border-input bg-background px-3 text-sm outline-none transition focus-visible:ring-2 focus-visible:ring-ring";
const buttonClass = "inline-flex h-9 items-center justify-center gap-2 rounded-lg border border-border bg-background px-3 text-xs font-medium transition hover:bg-accent disabled:pointer-events-none disabled:opacity-50";
const primaryClass = "inline-flex h-9 items-center justify-center gap-2 rounded-lg bg-primary px-3 text-xs font-medium text-primary-foreground transition hover:bg-primary/90 disabled:pointer-events-none disabled:opacity-50";

function useSavedAddresses() {
  const [warehouses, setWarehouses] = useState<SavedAddressRecord[]>([]);
  const [consignees, setConsignees] = useState<SavedAddressRecord[]>([]);
  useEffect(() => {
    let cancelled = false;
    void Promise.all([pssApi<{ data: Array<Record<string, string>> }>("/v1/warehouses"), pssApi<{ data: Array<Record<string, string>> }>("/v1/addresses")]).then(([warehouseResult, addressResult]) => { if (cancelled) return; const map = (row: Record<string, string>): SavedAddressRecord => ({ id: row.id, name: row.name ?? row.label ?? "", kind: "warehouse", company: "", address: { line: row.address ?? "", city: row.city ?? "", state: row.state ?? "", pincode: row.pincode ?? "", country: "India" }, contact: { name: row.contact_name ?? "", phone: row.phone ?? row.contact ?? "", email: "" }, default: false }); setWarehouses(warehouseResult.data.map(map)); setConsignees(addressResult.data.map(map)); }).catch(() => { if (!cancelled) { setWarehouses([]); setConsignees([]); } });
    return () => { cancelled = true; };
  }, []);
  return { warehouses, consignees };
}

function PageHeader({ title, description }: { title: string; description: string }) {
  return <div className="flex flex-wrap items-start justify-between gap-3"><div><div className="mb-1 text-[10px] font-semibold uppercase tracking-[0.18em] text-primary">Information Center</div><h1 className="text-xl font-semibold tracking-tight">{title}</h1><p className="mt-1 text-sm text-muted-foreground">{description}</p></div><span className="rounded-full border border-border bg-muted/40 px-2.5 py-1 text-[11px] font-medium text-muted-foreground">Production API</span></div>;
}

function AddressSelect({ label, records, value, onChange }: { label: string; records: SavedAddressRecord[]; value: string; onChange: (value: string) => void }) {
  return <label className="block space-y-1.5"><span className="text-xs font-medium">{label} <span className="font-normal text-muted-foreground">(optional)</span></span><select className={inputClass} value={value} onChange={(event) => onChange(event.target.value)}><option value="">Choose saved address</option>{records.map((record) => <option key={record.id} value={record.address.pincode}>{record.name} · {record.address.pincode}</option>)}</select></label>;
}

function PincodeField({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) {
  return <label className="block space-y-1.5"><span className="text-xs font-medium">{label}</span><input className={inputClass} inputMode="numeric" maxLength={6} placeholder="6-digit pincode" value={value} onChange={(event) => onChange(event.target.value.replace(/\D/g, "").slice(0, 6))} /></label>;
}

function Segment<T extends string>({ values, value, onChange }: { values: T[]; value: T; onChange: (value: T) => void }) {
  return <div className="inline-flex flex-wrap rounded-lg border border-border bg-muted/40 p-1">{values.map((item) => <button type="button" key={item} onClick={() => onChange(item)} className={cn("rounded-md px-3 py-1.5 text-xs transition", value === item ? "bg-background font-medium shadow-sm" : "text-muted-foreground hover:text-foreground")}>{item}</button>)}</div>;
}

export function PincodeServiceability() {
  const { warehouses, consignees } = useSavedAddresses();
  const [pickup, setPickup] = useState("");
  const [delivery, setDelivery] = useState("");
  const [status, setStatus] = useState<"idle" | "loading" | "done">("idle");
  const [error, setError] = useState("");
  const [result, setResult] = useState<ReturnType<typeof checkServiceability> | null>(null);
  const runCheck = () => {
    if (!isValidPincode(pickup) || !isValidPincode(delivery)) return setError("Enter valid six-digit pickup and delivery pincodes.");
    if (pickup === delivery) return setError("Pickup and delivery pincodes must be different.");
    setError(""); setStatus("loading");
    void pssApi<{ data: { serviceable: boolean; providers: string[]; status: string } }>("/v1/serviceability", { method: "POST", body: JSON.stringify({ origin_pincode: pickup, destination_pincode: delivery }) }).then((response) => { const available = response.data.serviceable; setResult({ pickup: available, delivery: available, modes: response.data.providers.map((provider) => provider === "delhivery" ? "Express" : "Surface"), eta: available ? "Provider confirmation required" : "Not available", prepaid: available, cod: available, reverse: available, temporary: !available, message: available ? "The route is eligible for configured courier providers." : "No courier provider is currently enabled for this route." }); setStatus("done"); }).catch((error) => { setError(error instanceof Error ? error.message : "Unable to check serviceability."); setStatus("idle"); });
  };
  const reset = () => { setPickup(""); setDelivery(""); setResult(null); setError(""); setStatus("idle"); };
  return <div className="w-full space-y-5"><PageHeader title="Pincode Serviceability" description="Check whether a pickup and delivery route is currently supported." /><div className="grid gap-4 xl:grid-cols-[minmax(0,0.85fr)_minmax(0,1.15fr)]"><section className="rounded-xl border border-border bg-card p-4"><div className="mb-4 flex items-center gap-2"><MapPin className="size-4 text-primary" /><h2 className="text-sm font-semibold">Route details</h2></div><div className="grid gap-3 sm:grid-cols-2"><PincodeField label="Pickup pincode" value={pickup} onChange={setPickup} /><PincodeField label="Delivery pincode" value={delivery} onChange={setDelivery} /><AddressSelect label="Pickup warehouse" records={warehouses} value="" onChange={setPickup} /><AddressSelect label="Delivery address" records={consignees} value="" onChange={setDelivery} /></div>{error && <p className="mt-3 rounded-lg border border-destructive/30 bg-destructive/10 px-3 py-2 text-xs text-destructive">{error}</p>}<div className="mt-4 flex flex-wrap gap-2"><button type="button" className={primaryClass} onClick={runCheck} disabled={status === "loading"}>{status === "loading" ? <RefreshCw className="size-3.5 animate-spin" /> : <Check className="size-3.5" />}Check serviceability</button><button type="button" className={buttonClass} onClick={() => { const next = pickup; setPickup(delivery); setDelivery(next); }}><ArrowRight className="size-3.5" />Swap</button><button type="button" className={buttonClass} onClick={reset}>Reset</button></div></section><section className="min-h-[340px] rounded-xl border border-border bg-card p-4" aria-live="polite">{status === "idle" && <div className="grid h-full min-h-[300px] place-items-center text-center"><div><MapPin className="mx-auto mb-3 size-8 text-muted-foreground/40" /><p className="text-sm font-medium">Check a route to see serviceability</p><p className="mt-1 text-xs text-muted-foreground">The result is returned by the production API.</p></div></div>}{status === "loading" && <div className="grid h-full min-h-[300px] place-items-center text-center"><RefreshCw className="mx-auto mb-3 size-7 animate-spin text-primary" /><p className="text-sm font-medium">Checking route...</p></div>}{status === "done" && result && <div className="space-y-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs text-muted-foreground">Overall route</p><h2 className="text-lg font-semibold">{!result.pickup || !result.delivery ? "Not serviceable" : result.temporary ? "Temporarily unavailable" : "Serviceable route"}</h2></div><span className={cn("rounded-full px-2.5 py-1 text-[11px] font-medium", result.temporary ? "bg-amber-500/15 text-amber-700 dark:text-amber-300" : "bg-emerald-500/15 text-emerald-700 dark:text-emerald-300")}>{result.temporary ? "Review required" : "Available"}</span></div><p className="text-sm text-muted-foreground">{result.message}</p><div className="grid gap-2 sm:grid-cols-2">{[["Pickup", result.pickup ? "Available" : "Unavailable"], ["Delivery", result.delivery ? "Available" : "Unavailable"], ["Modes", result.modes.join(" · ") || "None"], ["ETA", result.eta], ["Prepaid", result.prepaid ? "Available" : "Unavailable"], ["COD", result.cod ? "Available" : "Unavailable"], ["Reverse / RTO", result.reverse ? "Available" : "Unavailable"]].map(([label, value]) => <div key={String(label)} className="rounded-lg border border-border/70 bg-muted/20 p-3"><p className="text-[11px] text-muted-foreground">{label}</p><p className="mt-1 text-sm font-medium">{value}</p></div>)}</div><p className="border-t border-border pt-3 text-[11px] text-muted-foreground">Availability reflects the currently configured production courier providers.</p></div>}</section></div></div>;
}

function NumberInput({ value, onChange, min = 0, step = "1" }: { value: number | string; onChange: (value: string) => void; min?: number; step?: string }) {
  return <input className={inputClass} type="number" min={min} step={step} value={value} onChange={(event) => onChange(event.target.value)} />;
}

export function RateCheck() {
  const { warehouses, consignees } = useSavedAddresses();
  const [pickup, setPickup] = useState(""); const [delivery, setDelivery] = useState("");
  const [boxes, setBoxes] = useState<BoxLine[]>([]);
  const [deadWeight, setDeadWeight] = useState(""); const [shipmentValue, setShipmentValue] = useState(""); const [payment, setPayment] = useState<PaymentMode>("Prepaid"); const [codAmount, setCodAmount] = useState(""); const [shipmentType, setShipmentType] = useState<ShipmentType>("Forward"); const [mode, setMode] = useState("All");
  const [rates, setRates] = useState<ReturnType<typeof calculateRates> | null>(null); const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  const [quoteEnabled, setQuoteEnabled] = useState(false);
  useEffect(() => { void pssApi<{ data: Record<string, { enabled?: boolean }> }>("/v1/provider-capabilities").then((response) => setQuoteEnabled(Object.values(response.data).some((provider) => provider.enabled === true))).catch(() => setQuoteEnabled(false)); }, []);
  const boxCount = boxes.reduce((sum, box) => sum + (Number(box.quantity) || 0), 0); const volumetric = volumetricWeight(boxes); const chargeable = chargeableWeight({ boxes, deadWeight: Number(deadWeight) || 0 });
  const updateBox = (id: number, key: keyof BoxLine, value: string) => setBoxes((rows) => rows.map((row) => row.id === id ? { ...row, [key]: key === "id" ? row.id : Number(value) } : row));
  const calculate = () => { if (!quoteEnabled) return setError("Live courier quotes are unavailable until a verified provider rate contract is enabled."); if (!isValidPincode(pickup) || !isValidPincode(delivery)) return setError("Enter valid six-digit pickup and delivery pincodes."); if (pickup === delivery) return setError("Pickup and delivery pincodes must be different."); if (chargeable <= 0 || boxCount <= 0) return setError("Add at least one box and a positive dead weight."); if (payment === "COD" && Number(codAmount) <= 0) return setError("Enter a valid COD collectable amount."); setError(""); setLoading(true); void pssApi<{ data: { amount: number } }>("/v1/rate-quotes", { method: "POST", headers: { "Idempotency-Key": crypto.randomUUID() }, body: JSON.stringify({ origin_pincode: pickup, destination_pincode: delivery, weight_kg: chargeable, pieces: boxCount, provider: mode === "All" ? undefined : mode.toLowerCase() }) }).then((response) => { const amount = response.data.amount; setRates([{ mode: "Express", courier: "Production quote", zone: "Server calculated", chargeableWeight: chargeable, eta: "Provider confirmation required", freight: amount, cod: 0, fuel: 0, surcharge: 0, tax: 0, rto: 0, reverse: 0, total: amount, formattedTotal: formatINR(amount) }]); setLoading(false); }).catch((error) => { setError(error instanceof Error ? error.message : "Unable to calculate a production quote."); setLoading(false); }); };
  const reset = () => { setRates(null); setError(""); setPickup(""); setDelivery(""); setBoxes([]); setDeadWeight(""); setShipmentValue(""); setPayment("Prepaid"); setCodAmount(""); setMode("All"); };
  const visibleRates = rates?.filter((rate) => mode === "All" || rate.mode === mode) ?? [];
  return <div className="w-full space-y-5"><PageHeader title="Rate Check" description="Request a live courier quote when a verified provider rate contract is enabled." /><div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_minmax(0,0.9fr)]"><section className="space-y-4 rounded-xl border border-border bg-card p-4"><div className="flex items-center gap-2"><Calculator className="size-4 text-primary" /><h2 className="text-sm font-semibold">Shipment inputs</h2></div><div className="grid gap-3 sm:grid-cols-2"><PincodeField label="Pickup pincode" value={pickup} onChange={setPickup} /><PincodeField label="Delivery pincode" value={delivery} onChange={setDelivery} /><AddressSelect label="Pickup warehouse" records={warehouses} value="" onChange={setPickup} /><AddressSelect label="Delivery address" records={consignees} value="" onChange={setDelivery} /></div><div className="grid gap-3 sm:grid-cols-3"><label className="space-y-1.5"><span className="text-xs font-medium">Dead weight (kg)</span><NumberInput value={deadWeight} onChange={setDeadWeight} step="0.01" /></label><label className="space-y-1.5"><span className="text-xs font-medium">Shipment value (₹)</span><NumberInput value={shipmentValue} onChange={setShipmentValue} /></label><label className="space-y-1.5"><span className="text-xs font-medium">Payment mode</span><Segment values={["Prepaid", "COD"]} value={payment} onChange={setPayment} /></label></div>{payment === "COD" && <label className="block max-w-xs space-y-1.5"><span className="text-xs font-medium">COD collectable amount (₹)</span><NumberInput value={codAmount} onChange={setCodAmount} /></label>}<div className="flex flex-wrap items-center gap-3"><span className="text-xs font-medium">Shipment type</span><Segment values={["Forward", "RTO", "Reverse/DTO"]} value={shipmentType} onChange={setShipmentType} /></div><div className="space-y-3 rounded-lg border border-border/70 bg-muted/20 p-3"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold">Package boxes</p><p className="text-[11px] text-muted-foreground">{boxCount} boxes · {volumetric.toFixed(2)} kg volumetric · {chargeable.toFixed(2)} kg chargeable</p></div><button type="button" className={buttonClass} onClick={() => setBoxes((rows) => [...rows, { id: Date.now(), quantity: 1, length: 20, breadth: 15, height: 10 }])}><Plus className="size-3.5" />Add box</button></div>{boxes.map((box, index) => <div className="grid gap-2 sm:grid-cols-[72px_repeat(3,minmax(0,1fr))_32px]" key={box.id}><label className="space-y-1"><span className="text-[10px] text-muted-foreground">Qty</span><NumberInput value={box.quantity} onChange={(value) => updateBox(box.id, "quantity", value)} min={1} /></label><label className="space-y-1"><span className="text-[10px] text-muted-foreground">Length</span><NumberInput value={box.length} onChange={(value) => updateBox(box.id, "length", value)} min={1} /></label><label className="space-y-1"><span className="text-[10px] text-muted-foreground">Breadth</span><NumberInput value={box.breadth} onChange={(value) => updateBox(box.id, "breadth", value)} min={1} /></label><label className="space-y-1"><span className="text-[10px] text-muted-foreground">Height</span><NumberInput value={box.height} onChange={(value) => updateBox(box.id, "height", value)} min={1} /></label><button type="button" aria-label={`Remove box ${index + 1}`} className="mt-5 grid size-8 place-items-center rounded-lg text-muted-foreground hover:bg-destructive/10 hover:text-destructive disabled:opacity-30" disabled={boxes.length === 1} onClick={() => setBoxes((rows) => rows.filter((row) => row.id !== box.id))}><Trash2 className="size-3.5" /></button></div>)}</div>{error && <p className="rounded-lg border border-destructive/30 bg-destructive/10 px-3 py-2 text-xs text-destructive">{error}</p>}<div className="flex flex-wrap items-center gap-2"><button type="button" className={primaryClass} disabled={loading || !quoteEnabled} onClick={calculate}>{loading ? <RefreshCw className="size-3.5 animate-spin" /> : <Calculator className="size-3.5" />}{quoteEnabled ? "Request live quote" : "Live quote unavailable"}</button>{!quoteEnabled && <span className="text-[11px] text-muted-foreground">Enable a verified provider rate contract to request pricing.</span>}<button type="button" className={buttonClass} onClick={reset}>Reset</button></div></section><section className="min-h-[500px] rounded-xl border border-border bg-card p-4" aria-live="polite"><div className="mb-4 flex flex-wrap items-center justify-between gap-2"><div><h2 className="text-sm font-semibold">Provider quote</h2><p className="text-xs text-muted-foreground">{rates ? `${visibleRates.length} service options · ${chargeable.toFixed(2)} kg chargeable` : "No verified provider quote available"}</p></div>{rates && <Segment values={["All", "Surface", "Express"]} value={mode} onChange={setMode} />}</div>{loading && <div className="grid min-h-[400px] place-items-center text-center"><RefreshCw className="mb-2 size-7 animate-spin text-primary" /><p className="text-sm font-medium">Requesting provider quote...</p></div>}{!loading && !rates && <div className="grid min-h-[400px] place-items-center text-center"><Truck className="mx-auto mb-3 size-8 text-muted-foreground/40" /><p className="text-sm font-medium">Live quotes unavailable</p><p className="mt-1 text-xs text-muted-foreground">A courier rate contract must be verified and enabled before pricing is shown.</p></div>}{!loading && rates && <div className="space-y-3">{visibleRates.map((rate) => <RateResult key={rate.mode} rate={rate} />)}<p className="border-t border-border pt-3 text-[11px] text-muted-foreground">Quote returned by a verified production provider.</p></div>}</section></div></div>;
}

function RateResult({ rate }: { rate: ReturnType<typeof calculateRates>[number] }) {
  const [open, setOpen] = useState(false);
  return <article className="rounded-xl border border-border/80 p-3"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><Truck className="size-4 text-primary" /><p className="text-sm font-semibold">{rate.courier} · {rate.mode}</p></div><p className="mt-1 text-xs text-muted-foreground">{rate.eta} · {rate.chargeableWeight.toFixed(2)} kg chargeable · {rate.zone} zone</p></div><div className="text-right"><p className="text-lg font-semibold">{rate.formattedTotal}</p><p className="text-[11px] text-muted-foreground">estimated total</p></div></div><div className="mt-3 flex flex-wrap gap-2"><button type="button" className={buttonClass} onClick={() => setOpen((value) => !value)}>{open ? "Hide" : "Show"} freight breakup<ChevronDown className={cn("size-3.5 transition-transform", open && "rotate-180")} /></button><Link className={primaryClass} href="/dashboard/shipmentBooking">Use in booking<ArrowRight className="size-3.5" /></Link></div>{open && <div className="mt-3 grid gap-2 border-t border-border pt-3 sm:grid-cols-2">{[["Freight", rate.freight], ["COD fee", rate.cod], ["Fuel surcharge", rate.fuel], ["Other surcharge", rate.surcharge], ["Tax", rate.tax], ["RTO charge", rate.rto], ["Reverse/DTO", rate.reverse]].map(([label, amount]) => <div className="flex items-center justify-between text-xs" key={String(label)}><span className="text-muted-foreground">{label}</span><span>{formatINR(Number(amount))}</span></div>)}</div>}</article>;
}

export function RateCard() {
  const [providers, setProviders] = useState<Array<{ name: string; enabled: boolean; configured: boolean; health?: string; lastError?: string | null }>>([]);
  const [error, setError] = useState("");
  useEffect(() => { void pssApi<{ data: Record<string, { enabled?: boolean; configured?: boolean; health?: string; last_error_code?: string | null }> }>("/v1/provider-capabilities").then((response) => setProviders(Object.entries(response.data).map(([name, value]) => ({ name, enabled: value.enabled === true, configured: value.configured === true, health: value.health, lastError: value.last_error_code })))).catch((reason) => setError(reason instanceof Error ? reason.message : "Unable to load provider status.")); }, []);
 return <div className="w-full space-y-5"><PageHeader title="Rate Card Check" description="View courier capability status; live quotes appear only after provider rate contracts are verified." /><section className="rounded-xl border border-amber-500/30 bg-amber-500/10 p-5"><div className="flex items-start gap-3"><Truck className="mt-0.5 size-5 text-amber-700" /><div><h2 className="text-sm font-semibold text-amber-900">Live courier rate quotes are unavailable</h2><p className="mt-1 text-sm text-amber-800">Commercial rate cards are managed by authorized Admin and Master users. No browser-local or estimated quote is shown until a verified provider rate contract is enabled.</p></div></div></section><section className="rounded-xl border border-border bg-card p-4"><h2 className="text-sm font-semibold">Courier configuration</h2>{error && <p className="mt-3 rounded-lg border border-destructive/30 bg-destructive/10 px-3 py-2 text-xs text-destructive">{error}</p>}<div className="mt-3 grid gap-2 sm:grid-cols-2">{providers.map((provider) => { const degraded = provider.health === "degraded"; const checking = provider.health === "pending"; const label = degraded ? "Degraded" : checking ? "Checking" : provider.enabled ? "Enabled" : provider.configured ? "Configured, disabled" : "Not configured"; return <div className="flex items-center justify-between rounded-lg border border-border/70 bg-muted/20 p-3" key={provider.name}><div><span className="text-sm font-medium">{provider.name}</span>{degraded && provider.lastError ? <p className="mt-1 text-[10px] text-amber-700 dark:text-amber-300">{provider.lastError}</p> : null}</div><span className={cn("rounded-full px-2 py-1 text-[11px]", degraded ? "bg-amber-500/15 text-amber-700 dark:text-amber-300" : checking ? "bg-sky-500/15 text-sky-700 dark:text-sky-300" : provider.enabled ? "bg-emerald-500/15 text-emerald-700 dark:text-emerald-300" : "bg-muted text-muted-foreground")}>{label}</span></div>; })}{!providers.length && !error && <p className="text-sm text-muted-foreground">Loading provider configuration…</p>}</div></section></div>;
}
