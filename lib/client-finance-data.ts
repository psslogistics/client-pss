import companyData from "@/data/company-demo/company-data.json";
export type WalletTransaction = { id: string; date: string; reference: string; type: string; description: string; amount: number; direction: "credit" | "debit"; status: "Completed" | "Pending" | "Failed" | "Refunded"; balance: number };
export type BillingRecord = { id: string; bookingDate: string; pickupDate: string; deliveryDate: string; status: string; client: string; consignor: string; consignee: string; courier: string; origin: string; destination: string; declaredWeight: number; measuredWeight: number; billableWeight: number; baseCharge: number; weightCharge: number; tax: number; total: number; payment: string; cod: number; pod: string };
const parseWeight = (value: string) => { const parsed = Number.parseFloat(value.replaceAll(",", "")); return Number.isFinite(parsed) ? parsed : 1; }; const money = (value: number) => Math.round(value * 100) / 100;
export const billingRecords: BillingRecord[] = companyData.deliveries.slice(0, 60).map((record, index) => { const declaredWeight = parseWeight(record.weight); const measuredWeight = index % 5 === 0 ? declaredWeight + 2 : index % 7 === 0 ? Math.max(0.5, declaredWeight - 1) : declaredWeight; const billableWeight = Math.max(declaredWeight, measuredWeight); const baseCharge = money(180 + billableWeight * 28); const weightCharge = money(Math.max(0, billableWeight - declaredWeight) * 36); const tax = money((baseCharge + weightCharge) * 0.18); return { id: record.id, bookingDate: record.booked === "Not provided" ? record.updated : record.booked, pickupDate: record.booked === "Not provided" ? record.updated : record.booked, deliveryDate: record.status === "Delivered" ? record.updated : "Not delivered", status: record.status, client: record.client, consignor: record.origin, consignee: record.consignee, courier: record.courier, origin: record.origin, destination: record.destination, declaredWeight, measuredWeight, billableWeight, baseCharge, weightCharge, tax, total: money(baseCharge + weightCharge + tax), payment: record.payment, cod: record.payment.toLowerCase().includes("cod") || index % 9 === 0 ? money(record.amount * 0.35) : 0, pod: "POD unavailable" }; });
export const initialWalletTransactions: WalletTransaction[] = [{ id: "WLT-260815-001", date: "2026-08-15", reference: "Recharge", type: "Wallet recharge", description: "Frontend demo opening balance", amount: 50000, direction: "credit", status: "Completed", balance: 50000 }, { id: "WLT-260814-002", date: "2026-08-14", reference: billingRecords[2]?.id || "312200621", type: "Shipment booking", description: "Prepaid shipment booking", amount: 1380, direction: "debit", status: "Completed", balance: 48620 }, { id: "WLT-260813-003", date: "2026-08-13", reference: billingRecords[5]?.id || "313009257", type: "Weight difference", description: "Additional charge after courier reweigh", amount: 84.96, direction: "debit", status: "Completed", balance: 48535.04 }, { id: "WLT-260812-004", date: "2026-08-12", reference: billingRecords[8]?.id || "312138483", type: "Cancelled booking", description: "Refund for cancelled booking", amount: 620, direction: "credit", status: "Refunded", balance: 49155.04 }, { id: "WLT-260811-005", date: "2026-08-11", reference: billingRecords[11]?.id || "312015725", type: "Payment failed", description: "Payment failed after wallet debit", amount: 960, direction: "debit", status: "Failed", balance: 48195.04 }, { id: "WLT-260811-006", date: "2026-08-11", reference: "WLT-260811-005", type: "Payment refund", description: "Automatic refund for failed payment", amount: 960, direction: "credit", status: "Refunded", balance: 49155.04 }];
export const formatINR = (value: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", minimumFractionDigits: 2 }).format(value);
