const escapeXml = (value: unknown) => String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
const crc32 = (bytes: Uint8Array) => { let crc = 0xffffffff; for (const byte of bytes) { crc ^= byte; for (let i = 0; i < 8; i++) crc = (crc >>> 1) ^ (crc & 1 ? 0xedb88320 : 0); } return (crc ^ 0xffffffff) >>> 0; };
const columnName = (index: number) => { let result = ""; let value = index + 1; while (value) { const remainder = (value - 1) % 26; result = String.fromCharCode(65 + remainder) + result; value = Math.floor((value - 1) / 26); } return result; };
const zipStore = (files: { name: string; content: string }[]) => { const encoder = new TextEncoder(); const parts: Uint8Array[] = []; const central: Uint8Array[] = []; let offset = 0; const p16 = (v: DataView, i: number, n: number) => v.setUint16(i, n, true); const p32 = (v: DataView, i: number, n: number) => v.setUint32(i, n, true); files.forEach(({ name, content }) => { const nb = encoder.encode(name); const data = encoder.encode(content); const local = new Uint8Array(30 + nb.length + data.length); const lv = new DataView(local.buffer); p32(lv, 0, 0x04034b50); p16(lv, 4, 20); p32(lv, 14, crc32(data)); p32(lv, 18, data.length); p32(lv, 22, data.length); p16(lv, 26, nb.length); local.set(nb, 30); local.set(data, 30 + nb.length); parts.push(local); const dir = new Uint8Array(46 + nb.length); const dv = new DataView(dir.buffer); p32(dv, 0, 0x02014b50); p16(dv, 4, 20); p16(dv, 6, 20); p32(dv, 16, crc32(data)); p32(dv, 20, data.length); p32(dv, 24, data.length); p16(dv, 28, nb.length); p32(dv, 42, offset); dir.set(nb, 46); central.push(dir); offset += local.length; }); const size = central.reduce((sum, item) => sum + item.length, 0); const end = new Uint8Array(22); const ev = new DataView(end.buffer); p32(ev, 0, 0x06054b50); p16(ev, 8, files.length); p16(ev, 10, files.length); p32(ev, 12, size); p32(ev, 16, offset); const result = new Uint8Array(offset + size + 22); let cursor = 0; [...parts, ...central, end].forEach((part) => { result.set(part, cursor); cursor += part.length; }); return result; };
export type WorkbookSheet = { name: string; headers: string[]; rows: (string | number | boolean | null)[][] };
export function buildWorkbook(sheets: WorkbookSheet[]) { const sheetXml = (sheet: WorkbookSheet) => { const row = (values: (string | number | boolean | null)[], n: number) => `<row r="${n}">${values.map((value, index) => `<c r="${columnName(index)}${n}" t="inlineStr"><is><t>${escapeXml(value)}</t></is></c>`).join("")}</row>`; return `<worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><sheetData>${row(sheet.headers, 1)}${sheet.rows.map((values, index) => row(values, index + 2)).join("")}</sheetData></worksheet>`; }; const files = [{ name: "[Content_Types].xml", content: `<?xml version="1.0"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/><Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/>${sheets.map((_, i) => `<Override PartName="/xl/worksheets/sheet${i + 1}.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>`).join("")}</Types>` }, { name: "_rels/.rels", content: `<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>` }, { name: "xl/workbook.xml", content: `<workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets>${sheets.map((sheet, i) => `<sheet name="${escapeXml(sheet.name)}" sheetId="${i + 1}" r:id="rId${i + 1}"/>`).join("")}</sheets></workbook>` }, { name: "xl/_rels/workbook.xml.rels", content: `<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">${sheets.map((_, i) => `<Relationship Id="rId${i + 1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet${i + 1}.xml"/>`).join("")}</Relationships>` }, ...sheets.map((sheet, i) => ({ name: `xl/worksheets/sheet${i + 1}.xml`, content: sheetXml(sheet) }))]; return zipStore(files); }
export function downloadWorkbook(filename: string, sheets: WorkbookSheet[]) { const url = URL.createObjectURL(new Blob([buildWorkbook(sheets).buffer as ArrayBuffer], { type: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" })); const link = document.createElement("a"); link.href = url; link.download = filename; link.click(); window.setTimeout(() => URL.revokeObjectURL(url), 500); }
